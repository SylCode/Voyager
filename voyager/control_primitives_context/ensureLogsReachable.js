async function ensureLogsReachable(bot) {
    // Makes surface logs reachable before a wood-dependent step.
    // It leaves decorative spawn pockets, searches for natural log-like blocks,
    // and explores horizontally when only buried or decorative wood is nearby.
}